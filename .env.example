# Environment Variables for Production Deployment
# Copy these to your hosting platform's environment variables section

# MongoDB Connection String
MONGODB_URI=mongodb+srv://meetkavad07:KzevMFPWp7lp6owb@cluster0.yizudmm.mongodb.net/excitement-calendar?retryWrites=true&w=majority

# Port (usually set automatically by hosting platform)
PORT=3000

# Session Secret (generate a new one for production)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Authentication Password (change this to your preferred password)
AUTH_PASSWORD=mySecurePassword123

# For different deployment platforms:
# - Heroku: Set these in dashboard Settings > Config Vars
# - Vercel: Set in project settings > Environment Variables
# - Railway: Set in project settings > Variables
# - Render: Set in dashboard > Environment Variables
